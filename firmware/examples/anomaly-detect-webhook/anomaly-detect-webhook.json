{
    "event": "glowfish_anomaly_detect",
    "url": "https://<GLOWFISH AUTH KEY>:<GLOWFISH AUTH SECRET KEY>@api.glowfi.sh/v1/anomaly_detect/",
    "requestType": "POST",
    "mydevices": true,
    "json": {"data_set": {
                "temp": ["{{temp}}"],
                "photo": ["{{photo}}"],
                "wifi": ["{{wifi}}"]
              },
              "time": ["{{SPARK_PUBLISHED_AT}}"]
            }
}
